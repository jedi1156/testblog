.post__comments
	%header
		%h2
			Comments
			- logger.info defined? see_all_link
			- if defined? see_all_link
				%small
					= link_to "see all", post_comments_path(post)
			= link_to "new comment", new_post_comment_path(post), class: "comment__button--add pull-right"
	- comments.each do |comment|
		= render "comments/comment", comment: comment, post: post